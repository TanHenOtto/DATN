{
  "name": "health-nutrition-tracker",
  "version": "1.0.0",
  "description": "Ứng dụng theo dõi sức khỏe và dinh dưỡng với AI",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../ai && pip install -r requirements.txt"
  },
  "keywords": [
    "health",
    "nutrition",
    "tracking",
    "ai",
    "vietnamese-food",
    "calorie-tracking"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
